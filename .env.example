# Local runtime config. Copy to `.env.local` and fill only what you need.

# Feishu group bot webhook. Not required for `npm run start:dry`.
# FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxx

# Optional custom file paths. Relative paths are resolved from project root.
# PORTFOLIO_PATH=./data/portfolio.json
# STRATEGY_PATH=./data/strategy.json
# STATE_PATH=./state.json

# Optional direct JSON injection. Useful for GitHub Actions Secrets / Variables.
# When set, these take precedence over local files.
# PORTFOLIO_JSON=[{"id":"example"}]
# STRATEGY_JSON={"bands":[],"formula":{}}
# STATE_JSON={"lastTriggeredBandId":null,"lastRunAt":null,"lastObservedTemperature":null,"lastObservedYieldRate":null}

# Optional debug switches.
# DRY_RUN=1
# FORCE_TRIGGER=1
